{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the HMSI main window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}